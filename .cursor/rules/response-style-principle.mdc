---
description: 
alwaysApply: true
---

# 強制（違反視為未完成回答）

0. **不論使用者問什麼**：只要回覆需要指向、摘錄、解釋或依據本工作區內任一檔案之具體內容（含設定、規則、文件、腳本等），**一律**須符合下列「可點擊引用呈現標準」，不得以口頭描述檔名或行號代替：
   - **可點擊引用呈現標準**：請用可點擊的程式碼引用呈現：要包含完整（或相對）檔案路徑和起訖行號，方便使用者點程式碼區塊標題跳到編輯器那一行。
   - 若整則回覆**完全未**涉及本工作區檔案內容（純概念、與 repo 無關之一般說明），則無需強制引用；**一旦**提到某檔、某行、某段原文或修改位置，即適用本條。

1. **語言**：一律以繁體中文回覆（專有名詞、檔名、識別字可維持原文）。
2. **程式碼、邏輯、流程之解釋（與第 0 條一致）**：
   - 凡在解釋**程式碼**、**邏輯**或**流程**（含誰呼叫誰、資料流、條件分支、狀態變化），**一律**須在文中**列出實際程式片段**作為依據，不得以純文字敘述代替「可驗證的程式出處」。
   - **列出整段或連續多行**（足以還原語意之一段）時，**必須**使用下文第 4 條之格式，使柵欄第一行同時標明 **`起始行:結束行:檔案路徑`**，以利編輯器**一鍵跳轉**至該範圍；不得以無行號／無路徑之一般 code fence 代表「專案內某檔某段」。
3. **凡回覆涉及「程式碼、邏輯、行為、修改位置、逐行說明」或本工作區檔案之定錨說明**：**每個論點或每段說明**都必須附至少一則**可點擊的程式碼引用**，且引用須能對應到實際檔案與行號；**不得**只用口頭描述檔名或行號而無引用。（與第 0 條之「可點擊引用呈現標準」一致。）
4. **可點擊引用唯一允許格式**（Cursor 程式碼引用）：markdown fenced code 的**開頭柵欄單獨一行**，第一行內容為 `起始行:結束行:檔案路徑`（相對於 repo 根或完整路徑均可，須為編輯器可開啟之路徑）。範例：

```12:18:src/example.ts
// 範例：此為規則說明用假檔名，實際回答時請替換為真實路徑與行號
```

5. **禁止**：使用無 `起:迄:路徑` 的純 ``` 程式區塊來代表「專案內某檔某段」程式（例如僅標 `typescript` 或無資訊的 fence）；**禁止**在引用柵欄第一行與路徑前加列表符號或其他字元（會導致無法點擊）。

# 流程、架構與混合型問題（與「純寫程式」同等標準）

- **適用**：使用者問的是**流程**（誰呼叫誰、從路由到畫面、資料怎麼流）、**架構**、**除錯路徑**、或「流程 + 程式」混合題時，**仍須**遵守上文「可點擊引用」、第 2 條「須列出實際程式片段」與繁中要求；不得以「這題不是寫 code」為由只講故事不附出處。
- **建議結構**：
  1. **結論先行**：用短段落寫主路徑（入口 → 關鍵步驟 → 出口或結果）。
  2. **可驗證依據**：每個關鍵步驟對應到**檔案路徑 + 起訖行號**，並以**可點擊引用**呈現；跨多檔時用**編號清單**逐步對應「步驟 ↔ 程式位置」。
  3. **隱含邏輯**：框架生命週期、延遲求值、`$this` 在 view 的意義、事件／hook、快取與設定覆寫等，凡影響理解處須簡要點出。
- **視覺輔助**：僅在分支多、角色多、狀態多時使用 mermaid 或類似圖；簡單流程以文字 + 引用即可。
- **回答長度**：與問題複雜度成比例；先結論再細節，避免冗長堆疊與題無關內容。

# 其餘約定（仍須遵守）

- **修改程式**：須指明插入／修改的檔案與位置，並附上該處（或相鄰）之可點擊引用。
- **逐行解釋**：以新手可理解方式逐行說明；每一行或每一小段仍須有對應之可點擊引用（可同一引用內用註解標示行意，必要時拆成多則引用）；並補充**特殊語法**與**隱含邏輯**（與「流程」節一致）。
- **無法讀檔或無對應程式時**：明確說明原因；**仍不得**用假行號偽造可點擊引用。
