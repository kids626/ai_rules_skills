---
description: 使用者輸入 savesop 時依 save-template-sop skill 產出流程摘要並落檔
alwaysApply: true
---

# savesop 觸發

當使用者**本則訊息**在去除首尾空白後、**不分大小寫**等於 `savesop` 時：

1. 依專案內 `skills/ActiveTrigger/save-template-sop/SKILL.md` 的「作業順序」與「輸出範本」執行。
2. 摘要範圍為**截至目前為止的本對話**（含程式流程、模板渲染鏈、後端 API 鏈等該 skill 要求之內容）。
3. 將完整 Markdown **寫入** repo 根目錄 `md文件檔/local_notes/`；目錄若不存在則先建立。
4. 回覆中註明**實際儲存檔案路徑**，勿只回覆確認而略過產檔。

若訊息除 `savesop` 外還有其他文字，則**不**啟用本條（改依一般對話與 skill 描述判斷）。
