---
description: 回覆與程式碼說明規則（繁中，強制可點擊引用）
globs:
alwaysApply: true
---

# 強制（違反視為未完成回答）

1. **語言**：一律以繁體中文回覆（專有名詞、檔名、識別字可維持原文）。
2. **凡使用者問題涉及「程式碼、邏輯、行為、修改位置、逐行說明」任一項**：**每個論點或每段說明**都必須附至少一則**可點擊的程式碼引用**，且引用須能對應到實際檔案與行號；**不得**只用口頭描述檔名或行號而無引用。
3. **可點擊引用唯一允許格式**（Cursor 程式碼引用）：markdown fenced code 的**開頭柵欄單獨一行**，第一行內容為 `起始行:結束行:檔案路徑`（相對於 repo 根或完整路徑均可，須為編輯器可開啟之路徑）。範例：

```12:18:src/example.ts
// 範例：此為規則說明用假檔名，實際回答時請替換為真實路徑與行號
```

4. **禁止**：使用無 `起:迄:路徑` 的純 ``` 程式區塊來代表「專案內某檔某段」程式（例如僅標 `typescript` 或無資訊的 fence）；**禁止**在引用柵欄第一行與路徑前加列表符號或其他字元（會導致無法點擊）。

# 其餘約定（仍須遵守）

- **修改程式**：須指明插入／修改的檔案與位置，並附上該處（或相鄰）之可點擊引用。
- **逐行解釋**：以新手可理解方式逐行說明；每一行或每一小段仍須有對應之可點擊引用（可同一引用內用註解標示行意，必要時拆成多則引用）。
- **無法讀檔或無對應程式時**：明確說明原因；**仍不得**用假行號偽造可點擊引用。
